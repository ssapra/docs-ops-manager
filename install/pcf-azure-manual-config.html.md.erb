---
title: Installing Ops Manager on Azure Manually
owner: Ops Manager
---

<% current_page.data.title = "Installing " + vars.platform_name + " on Azure Manually" %>

This topic explains how to install <%= vars.platform_name %> on Microsoft Azure manually.

To install <%= vars.platform_name %> on Azure manually, do the following:

1. Deploy <%= vars.ops_manager %>. See [Deploying <%= vars.ops_manager %> on Azure Manually](/platform/ops-manager/<%= product_info['local_product_version'].to_s.sub('.','-') %>/azure/deploy-manual.html).

1. Configure BOSH Director. See [Configuring BOSH Director on Azure Manually](/platform/ops-manager/<%= product_info['local_product_version'].to_s.sub('.','-') %>/azure/config-manual.html).

1. Configure <%= vars.app_runtime_full %>. See [Configuring <%= vars.app_runtime_abbr %>](https://docs.pivotal.io/application-service/<%= product_info['local_product_version'].to_s.sub('.','-') %>/operating/configure-pas.html).
