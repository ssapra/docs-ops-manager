(Optional) Enter your comma-separated custom **Identification Tags.** For example, `iaas:foundation1, hello:world`. You can use the tags to identify your foundation when viewing VMs or disks from your IaaS.<% if current_page.data.iaas == "Azure" %><p class="note"><strong>Note:</strong> Azure has limited space for identification tags. <%= vars.company_name %> recommends entering no more than five tags.</p><% end %>
